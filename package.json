{
  "name": "contentdividend-cloudflare-authorization-bridge",
  "version": "1.1.0",
  "private": true,
  "description": "Deployable Cloudflare Worker template for ContentDividend publisher authorization.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "cloudflare": {
    "bindings": {
      "CONTENTDIVIDEND_BRIDGE_SECRET": {
        "description": "Paste the one-time private Bridge Secret from ContentDividend. Keep this secret private."
      }
    }
  }
}